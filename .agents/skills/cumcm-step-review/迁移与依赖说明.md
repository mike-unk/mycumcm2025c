# 迁移到其他工具 / 机器的依赖与说明

> 先运行 `python scripts/check_env.py`（退出码 0 = 核心齐全，2 = 仅缺可选工具）。本文件说明"复制包到别的工具效果变差"的常见原因——**通常不是包里缺文件，而是外部环境或工具加载方式不同**。

## 一、包本身是完整的

本技能所有内容（`SKILL.md`、`references/`、`scripts/`、`tools/`、`assets/`、`agents/`、摘要语料）都在包内。复制到其他工具后效果变差，先按下面两类原因排查，再怀疑缺文件。

## 二、外部环境（目标机器必须满足）

### Python 3.9+ 与核心包（见 `requirements.txt`）

```bash
pip install -r requirements.txt
```

核心包：numpy、scipy、pandas、matplotlib、scikit-learn、openpyxl、python-docx、lxml、seaborn、statsmodels、pypdf、pdfplumber、pillow。

### 外部程序（影响对应功能）

| 程序 | 作用 | 缺失后果 |
| :--- | :--- | :--- |
| xelatex + ctex（TeX Live / MiKTeX） | 生成 `完整论文.pdf` | LaTeX 分支无法编译 |
| Pandoc | LaTeX → Word 转换 | 完整论文 Word 版无法转换 |
| draw.io 桌面版 CLI | 流程图导出 | 流程图只能用浏览器降级/源码交付 |
| pdftoppm / mutool | PDF 渲染抽检 | 最终视觉检查受限 |
| LibreOffice（soffice） | Excel 公式重算 | Excel 重算受限 |
| Graphviz（dot） | drawio 自动布局（可选） | 大图需手排坐标 |
| MATLAB | `.m` 脚本（可选） | 仅 MATLAB 路径不可用 |

### 中文字体

- Windows：宋体/黑体/微软雅黑默认齐全。
- macOS/Linux：需安装 **Noto Sans CJK / Source Han Sans**，否则图表与 PDF 中文缺字、乱码。

### 网络

- `tools/paper_search/` 需要访问 OpenAlex / AnySearch；离线环境该工具不可用，但不影响其余部分。

## 三、工具差异（不是包的问题）

- **子技能加载**：Codex 会把 `tools/*/SKILL.md`（DOCX/LaTeX/PDF/Excel/论文搜索/drawio）注册为子技能，并按 SKILL.md 的"渐进式加载表"调用。其他工具若只认根 `SKILL.md`，需确认它支持子技能目录，或按 `tools/README.md` 手动把对应工具文档加入上下文。
- **`<SKILL_ROOT>` 占位符**：技能文档用 `<SKILL_ROOT>` 指代技能根目录；目标工具需能解析为实际路径（或执行时手动替换）。
- **`agents/openai.yaml`**：OpenAI 风格界面配置（display_name/default_prompt），其他工具会忽略，不影响功能。
- **`manifest.sha256.json`**：包清单，供校验用；其他工具忽略，不影响功能。
- **模型遵循度**：本技能依赖"先读文档、逐项门禁、未过不进下一步"的强流程；换模型/工具后如果对方不严格执行这些指令，效果会下降，属正常现象。可按需把指令拆小、逐步喂入。

## 四、迁移验证步骤

1. 安装依赖：`pip install -r requirements.txt`
2. 运行 `python scripts/check_env.py`，退出码 0 或 2（仅缺可选工具）才继续。
3. 逐项验证工具：DOCX（python-docx 可建文档）、LaTeX（xelatex 编译小样例）、PDF、Excel、论文搜索（网络）、drawio（`drawio --version`）。
4. 用一道小题跑通"数据预处理 → 建模 → 求解 → 写入 Word 草稿"全流程，再开始正式题目。

## 五、常见"效果变差"排查表

| 现象 | 原因 | 解决 |
| :--- | :--- | :--- |
| 脚本报 ModuleNotFoundError | 目标机器缺 Python 包 | `pip install -r requirements.txt` |
| 流程图不导出 | 未装 draw.io CLI | 安装 draw.io 桌面版；或走浏览器降级 |
| Word 版无法转换 | 缺 Pandoc | 安装 Pandoc |
| 图 / PDF 中文乱码 | 缺中文字体 | 安装 Noto Sans CJK |
| 论文 PDF 不编译 | 缺 TeX Live / MiKTeX | 安装 xelatex + ctex |
| 工具不执行子技能 | 工具只认根 SKILL.md | 手动加载 `tools/*/SKILL.md` 或换支持子技能的工具 |
| 流程执行不严格 | 模型遵循度差异 | 指令拆小、逐步喂入，或回到 Codex 使用 |
